# Database Viva Questions & Answers (Pets Care Management System)

এই ফাইলে আপনার প্রোজেক্টের ডাটাবেসের ওপর ভিত্তি করে ভাইভার জন্য ৫০টি গুরুত্বপূর্ণ প্রশ্ন ও উত্তর দেওয়া হলো।

## পর্ব ১: ডাটাবেস বেসিকস (Database Basics)
**১. ডাটাবেস (Database) কী?**
উত্তর: ডাটাবেস হলো ইন্টার-রিলেটেড ডেটার একটি সংগ্রহ, যা এমনভাবে সাজানো থাকে যাতে সহজেই ডেটা রাখা, খোঁজা এবং আপডেট করা যায়।

**২. DBMS কী?**
উত্তর: DBMS (Database Management System) হলো এমন একটি সফটওয়্যার যা ব্যবহারকারীদের ডাটাবেস তৈরি, পরিচালনা এবং কন্ট্রোল করতে সাহায্য করে। যেমন: MySQL, Oracle.

**৩. RDBMS কী? তোমার প্রোজেক্টে কোন ডাটাবেস ব্যবহার করেছো?**
উত্তর: RDBMS মানে Relational Database Management System। যেখানে ডেটা টেবিল (সারি এবং কলাম) আকারে সেভ থাকে। আমার প্রোজেক্টে আমি MySQL ব্যবহার করেছি যা একটি RDBMS।

**৪. টেবিল (Table) কাকে বলে?**
উত্তর: টেবিল হলো ডেটাবেসের একটি স্ট্রাকচার যা সারি (Row) এবং কলাম (Column) এর সমন্বয়ে তৈরি হয়।

**৫. রো (Row) এবং কলাম (Column) এর অন্য নাম কী?**
উত্তর: Row-কে Tuple বা Record বলা হয়। আর Column-কে Attribute বা Field বলা হয়।

**৬. Schema (স্কিমা) কী?**
উত্তর: স্কিমা হলো ডাটাবেসের লজিক্যাল ডিজাইন বা কাঠামো। অর্থাৎ টেবিলে কী কী কলাম থাকবে, প্রাইমারি কি কোনটা হবে, এসবের ডিজাইন।

**৭. ডেটা টাইপ (Data Type) বলতে কী বোঝায়? তোমার টেবিলে কী কী ডেটা টাইপ আছে?**
উত্তর: ডেটা টাইপ নির্ধারণ করে একটি কলামে কোন ধরনের ডেটা থাকবে। আমার ডাটাবেসে INT, VARCHAR, DOUBLE, এবং DATETIME ডেটা টাইপ ব্যবহার করা হয়েছে।

**৮. VARCHAR এবং CHAR এর মধ্যে পার্থক্য কী?**
উত্তর: CHAR ফিক্সড-লেংথ (নির্দিষ্ট মেমোরি নেয়), কিন্তু VARCHAR ভ্যারিয়েবল-লেংথ (যতটুকু ডেটা ততটুকু মেমোরি নেয়)।

**৯. NULL মান (NULL Value) বলতে কী বোঝায়?**
উত্তর: ডাটাবেসে NULL মানে হলো শূন্যতা বা অজানা ডেটা। এটি 0 বা স্পেস (Space) এর সমান নয়।

**১০. ডাটাবেসে ইনডেক্স (Index) কেন ব্যবহার করা হয়?**
উত্তর: অনেক ডেটার মধ্যে থেকে খুব দ্রুত ডেটা খুঁজে বের করার জন্য (Search fast) Index ব্যবহার করা হয়।

## পর্ব ২: কিস এবং রিলেশনশিপ (Keys and Relationships)
**১১. Primary Key (প্রাইমারি কি) কী?**
উত্তর: যে কলামের মান দিয়ে টেবিলের প্রতিটি রেকর্ডকে আলাদাভাবে (uniquely) চেনা যায় তাকে প্রাইমারি কি বলে। এটি কখনো NULL হতে পারে পল না।

**১২. Foreign Key (ফরেন কি) কী?**
উত্তর: ফরেন কি হলো একটি টেবিলের এমন একটি কলাম যা অন্য টেবিলের প্রাইমারি কি-কে রেফার করে। এটি টেবিলের মাঝে সম্পর্ক তৈরি করে।

**১৩. Unique Key এবং Primary Key এর মধ্যে পার্থক্য কী?**
উত্তর: প্রাইমারি কি তে কোনো NULL মান রাখা যায় না, কিন্তু ইউনিক কি তে একটি NULL মান রাখা যায়। একটি টেবিলে প্রাইমারি কি একটি থাকে, কিন্তু ইউনিক কি একাধিক থাকতে পারে।

**১৪. Composite Key কী?**
উত্তর: যখন একটি কলাম দিয়ে প্রাইমারি কি তৈরি করা সম্ভব হয় না, তখন দুই বা ততোধিক কলাম মিলিয়ে যে কি তৈরি হয় তাকে Composite Key বলে।

**১৫. ডাটাবেস রিলেশনশিপ কত প্রকার ও কী কী?**
উত্তর: তিন প্রকার: One-to-One, One-to-Many এবং Many-to-Many.

**১৬. তোমার প্রোজেক্টে One-to-Many রিলেশনশিপের একটি উদাহরণ দাও।**
উত্তর: একজন Customer একাধিক Order করতে পারে। তাই Customer এবং Orders টেবিলের মধ্যে One-to-Many রিলেশনশিপ আছে।

**১৭. Many-to-Many রিলেশনশিপ কীভাবে হ্যান্ডেল করা হয়?**
উত্তর: Many-to-Many রিলেশনশিপ সরাসরি হ্যান্ডেল করা যায় না। এর জন্য মাঝে একটি তৃতীয় টেবিল (Junction Table) তৈরি করতে হয়।

**১৮. ER Diagram কী?**
উত্তর: ER (Entity-Relationship) Diagram হলো ডাটাবেসের টেবিলগুলোর এবং তাদের মধ্যকার সম্পর্কের একটি চিত্র বা নকশা।

**১৯. Entity এবং Attribute বলতে কী বোঝায়?**
উত্তর: Entity হলো বাস্তব জগতের কোনো বস্তু (যেমন: Pet, Customer), আর Attribute হলো সেই বস্তুর বৈশিষ্ট্য (যেমন: Pet_Name, Age)।

**২০. Referential Integrity কী?**
উত্তর: এটি নিশ্চিত করে যে দুটি টেবিলের মধ্যে ফরেন কি এর মাধ্যমে তৈরি করা সম্পর্ক সবসময় সঠিক (valid) থাকে।

## পর্ব ৩: SQL কমান্ডস (SQL Commands)
**২১. SQL এর পূর্ণরূপ কী?**
উত্তর: Structured Query Language.

**২২. DDL, DML, DCL, TCL বলতে কী বোঝো?**
উত্তর: 
- DDL (Data Definition Language): CREATE, ALTER, DROP
- DML (Data Manipulation Language): INSERT, UPDATE, DELETE
- DCL (Data Control Language): GRANT, REVOKE
- TCL (Transaction Control Language): COMMIT, ROLLBACK

**২৩. DROP এবং TRUNCATE এর মধ্যে পার্থক্য কী?**
উত্তর: DROP টেবিলের স্ট্রাকচারসহ সব ডেটা ডিলিট করে দেয়। আর TRUNCATE শুধু টেবিলের ভেতরের সব ডেটা ডিলিট করে, কিন্তু টেবিলের স্ট্রাকচার রেখে দেয়।

**২৪. DELETE এবং TRUNCATE এর মধ্যে পার্থক্য কী?**
উত্তর: DELETE নির্দিষ্ট কোনো ডেটা ডিলিট করতে পারে (WHERE দিয়ে) এবং Rollback করা যায়। কিন্তু TRUNCATE সব ডেটা একসাথে ডিলিট করে এবং Rollback করা যায় না।

**২৫. SELECT কমান্ডের কাজ কী?**
উত্তর: টেবিল থেকে ডেটা খুঁজে বের করে আনার জন্য SELECT ব্যবহার করা হয়।

**২৬. UPDATE কমান্ড কীভাবে লেখে?**
উত্তর: `UPDATE table_name SET column1 = value1 WHERE condition;`

**২৭. INSERT কমান্ড কীভাবে লেখে?**
উত্তর: `INSERT INTO table_name (col1, col2) VALUES (val1, val2);`

**২৮. ALTER কমান্ডের কাজ কী?**
উত্তর: টেবিলের স্ট্রাকচার পরিবর্তন করতে (যেমন নতুন কলাম যোগ করা, ডেটা টাইপ পরিবর্তন করা) ALTER ব্যবহার হয়।

**২৯. WHERE ক্লজ (Clause) কেন ব্যবহার করা হয়?**
উত্তর: কোনো নির্দিষ্ট শর্ত বা কন্ডিশন দিয়ে ডেটা ফিল্টার করার জন্য WHERE ব্যবহার করা হয়।

**৩০. DISTINCT কিওয়ার্ডের কাজ কী?**
উত্তর: ডুপ্লিকেট ডেটা বাদ দিয়ে শুধু ইউনিক ডেটা দেখানোর জন্য DISTINCT ব্যবহার করা হয়।

## পর্ব ৪: অ্যাডভান্সড SQL এবং ফাংশনস (Advanced SQL & Functions)
**৩১. JOIN কী? কত প্রকার?**
উত্তর: একাধিক টেবিল থেকে ডেটা একসাথে মিলিয়ে আনার জন্য JOIN ব্যবহার করা হয়। এটি মূলত ৪ প্রকার: INNER, LEFT, RIGHT, এবং FULL JOIN।

**৩২. INNER JOIN কীভাবে কাজ করে?**
উত্তর: এটি শুধুমাত্র সেই ডেটাগুলো দেখায় যা দুটি টেবিলের মধ্যেই মিলে যায় (Match করে)।

**৩৩. LEFT JOIN এর কাজ কী?**
উত্তর: এটি বাম দিকের টেবিলের সব ডেটা দেখাবে এবং ডান দিকের টেবিলের শুধু ম্যাচ করা ডেটা দেখাবে।

**৩৪. GROUP BY ক্লজ এর কাজ কী?**
উত্তর: একই ধরনের ডেটাকে গ্রুপ করে দেখানোর জন্য (সাধারণত Aggregate Function এর সাথে) GROUP BY ব্যবহার করা হয়।

**৩৫. HAVING এবং WHERE এর মধ্যে পার্থক্য কী?**
উত্তর: WHERE সাধারণ কলামের ওপর শর্ত দেয়। আর HAVING ব্যবহার করা হয় GROUP BY করার পর Aggregate ফাংশনের ওপর শর্ত দেওয়ার জন্য।

**৩৬. SQL-এ Aggregate Function কোনগুলো?**
উত্তর: COUNT(), SUM(), AVG(), MIN(), MAX().

**৩৭. ORDER BY এর কাজ কী?**
উত্তর: ডেটাকে ছোট থেকে বড় (ASC) বা বড় থেকে ছোট (DESC) আকারে সাজানোর জন্য ORDER BY ব্যবহার করা হয়।

**৩৮. LIKE অপারেটরের কাজ কী?**
উত্তর: নির্দিষ্ট প্যাটার্ন অনুযায়ী ডেটা খোঁজার জন্য LIKE ব্যবহার হয়। যেমন নামের শুরুতে 'A' আছে এমন ডেটা খুঁজতে।

**৩৯. % এবং _ (Wildcards) এর পার্থক্য কী?**
উত্তর: % (পার্সেন্টেজ) যেকোনো সংখ্যক ক্যারেক্টার বোঝায়, আর _ (আন্ডারস্কোর) শুধুমাত্র একটি সিঙ্গেল ক্যারেক্টার বোঝায়।

**৪০. Subquery (সাবকুয়েরি) কী?**
উত্তর: একটি SQL কুয়েরির ভেতরে যখন অন্য আরেকটি কুয়েরি লেখা হয়, তখন তাকে Subquery বা Nested Query বলে।

## পর্ব ৫: প্রোজেক্ট স্পেসিফিক প্রশ্ন (Project Specific Questions)
**৪১. তোমার সিস্টেমে কেউ একটি Pet অ্যাডপ্ট করতে চাইলে ডাটাবেসে কী পরিবর্তন হবে?**
উত্তর: প্রথমে `adoptionrequests` টেবিলে একটি নতুন রিকোয়েস্ট INSERT হবে। রিকোয়েস্ট অ্যাপ্রুভ হলে Pet টেবিলের স্ট্যাটাস আপডেট হয়ে যাবে।

**৪২. কতজন কাস্টমার সিস্টেমে রেজিস্ট্রেশন করেছে তা কীভাবে বের করবে?**
উত্তর: `SELECT COUNT(*) FROM customer;`

**৪৩. সবচেয়ে দামি Pet কোনটি, তা কীভাবে বের করবে?**
উত্তর: `SELECT * FROM pet ORDER BY Price DESC LIMIT 1;`

**৪৪. আজকে কত টাকার Donation এসেছে তা কীভাবে বের করবে?**
উত্তর: `SELECT SUM(amount) FROM donation WHERE date = CURRENT_DATE;`

**৪৫. তোমার সিস্টেমে 'Doctor' রোলে কতজন Employee আছে তা কীভাবে দেখবে?**
উত্তর: `SELECT COUNT(*) FROM employee WHERE Role = 'Doctor';`

**৪৬. কোনো কাস্টমার তার পাসওয়ার্ড ভুলে গেলে ডাটাবেসে কীভাবে পরিবর্তন করবে?**
উত্তর: `UPDATE customer SET password = 'new_password' WHERE username = 'customer_username';`

**৪৭. Adoption Request পেন্ডিং আছে এমন লিস্ট কীভাবে দেখাবে?**
উত্তর: `SELECT * FROM adoptionrequests WHERE Status = 'Pending';`

**৪৮. কাস্টমার এবং তার অর্ডারগুলো একসাথে কীভাবে দেখাবে?**
উত্তর: `customer` এবং `orders` টেবিলের মাঝে JOIN চালিয়ে দেখাতে হবে।

**৪৯. ডাটাবেসে ছবি (Image) কীভাবে সেভ করেছো?**
উত্তর: ডাটাবেসে সরাসরি ছবি সেভ না করে ছবির নাম বা পাথ (Image Path) VARCHAR হিসেবে সেভ করেছি, আর মূল ছবিটি সার্ভারের ফোল্ডারে আছে।

**৫০. তোমার ডাটাবেসের টেবিলগুলোর নাম বলো।**
উত্তর: admin, adoptionpets, adoptionrequests, customer, donation, emergencyapplications, employee, medicines, orders, paymentconfig, pet, products, rescueteam, treatments.
